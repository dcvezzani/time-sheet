<%= form_for(@time_sheet_entry, html: {class: "form form-horizontal #{form_type(@time_sheet_entry)}"}) do |f| %>
  <% if @time_sheet_entry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@time_sheet_entry.errors.count, "error") %> prohibited this time_sheet_entry from being saved:</h2>

      <ul>
      <% @time_sheet_entry.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field control-group">
    <%= f.label :starts_at, class: "control-label" %>
    <div class="controls">
      <%= f.datetime_select :starts_at %>
    </div>
  </div>
  <div class="field control-group">
    <%= f.label :ends_at, class: "control-label" %>
    <div class="controls">
      <%= f.datetime_select :ends_at %>
    </div>
  </div>
  <div class="field control-group">
    <%= f.label :starts_at, class: "control-label" %>
    <div class="controls">
      <%= f.datetime_select :starts_at %>
    </div>
  </div>
  <div class="field control-group">
    <%= f.label :user_id, class: "control-label" %>
    <div class="controls">
      <%= f.number_field :user_id %>
    </div>
  </div>
  <div class="field control-group">
    <%= f.label :summary, class: "control-label" %>
    <div class="controls">
      <%= f.text_area :summary %>
    </div>
  </div>
  <div class="field control-group">
    <%= f.label :content, class: "control-label" %>
    <div class="controls">
      <%= f.text_area :content %>
    </div>
  </div>
  <div class="actions">
    <%= f.submit nil, class: "btn btn-primary" %>
  </div>
<% end %>
